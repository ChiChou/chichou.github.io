# AGENTS.md

This project is a static blog system in Next.js

## Setup commands

* Prefer `bun` on local development env
* When build to test frontend code, prefer `next build` to skip building optimized images to save time, unless we need explictly test optimized images.

## Frontend

* Prefer tailwind for style
* Prefer 'use client' components for interactivity
